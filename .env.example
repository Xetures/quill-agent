# 复制本文件为 .env 后再按需修改；.env 已被 git 忽略，不会提交
# cp .env.example .env

# 应用基础配置
APP_NAME=quill

# 数据根目录：data/、prompt/、skills/ 都挂在它下面（见 README 4.1）。
#
# 不配的话自动判断：当前目录下有 data/ 或 prompt/ 就用当前目录（就地运行），
# 否则用 ~/.quill。双击启动 / 打包分发的场景走的是后者，一般不用配这一项；
# 想把数据放到别处（比如移动硬盘、或者随项目走的目录）时才需要。
# QUILL_HOME=/path/to/quill-data

# 文件工具的工作目录，同时是安全边界：模型给出的路径一律限制在它里面。
#
# 不配的话，默认是**启动进程时所在的目录** —— 换个目录启动就等于换了个沙箱根目录。
# 正式使用时建议显式配上。
# WORK_DIR=/Users/you/projects/my-project

# 模型连接（base_url / api_key / 模型名）不在这里配：
# 它们存在 data/models.json 里，由界面「模型」页维护，改完即时生效、不用重启。

# 沙箱：给执行类工具（run_command / start_process）加一层内核级边界。
#
# 它和「危险命令先问用户」那套审批是两回事：沙箱管「命令够得着什么」，审批管「要不要问」。
#
#   off              不套沙箱（默认）—— 执行类命令不受工作目录限制
#   read-only        工作区只读、默认断网
#   workspace-write  工作区可写、默认断网
#
# 默认是 off，因为目前只有 Linux（bubblewrap）后端。配了沙箱但本机没有可用后端时，
# 命令会被**拒绝执行**（不会偷偷裸跑），所以别在 macOS / Windows 上打开它 ——
# 那会让每条命令都失败。写错的值会在启动时直接报错。
# SANDBOX_MODE=workspace-write

# 沙箱内是否放行网络。默认断网：数据外传是这类 Agent 最实际的风险。
# 装依赖那一下确实需要网，可以临时打开，装完再关回去。
# SANDBOX_NETWORK=false
