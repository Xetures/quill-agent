# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.20] - 2026-09-22

### 新增

- **桌面壳：同一个后端，装进一个原生窗口**（`quill_agent/desktop.py`，入口
  `quill-desktop`）。

  它替代的是「浏览器」那一层 —— 起同一个 uvicorn、打开同一个地址，容器换成系统
  WebView（macOS WKWebView、Windows WebView2、Linux WebKitGTK）。所以**前端与后端
  一行都没改**，而两条路可以并存：调试时照旧用浏览器打开同一个地址。

  三个行为是这一版真正要立住的，每个都有对应的实测：

  | 行为 | 为什么这么做 |
  | --- | --- |
  | **重复启动复用已有实例** | 两个后端共享同一份数据文件，存储层的锁只保证文件不写坏，不保证用户不会看到两个界面各说各话。判据是 `/api/health` 的应答里带 `status`/`version`，不是「端口通不通」—— 后者会把碰巧占了同一端口的程序认成自己人 |
  | **窗口等服务应答了才开** | 首启要播种、迁移、连 MCP，耗时不确定；按 `/api/health` 轮询，猜秒数不是早了（白页）就是晚了（启动变慢）。服务若自己退了（端口被抢、app 导入报错）立即放弃，不等满超时 |
  | **关窗带走服务** | 走 uvicorn 的 `should_exit` 而非杀线程：lifespan 的收尾在那里跑，MCP 用 stdio 起的子进程靠它收。实测关窗后端口释放、无残留进程 |

  另外两处为「双击启动没有终端」准备：输出重定向到数据根下的 `desktop.log`（启动自检
  与报错只能落到文件里，否则排障无从下手），以及「窗口已创建」那行日志（用来区分
  「服务没起来」和「窗口没开」）。

  pywebview 是**可选依赖**（`uv sync --extra desktop`）：不做桌面版的人不该为一个
  WebView 绑定多装一层，命令行与浏览器版照旧。

- **体积裁剪：187 MB → 101 MB**（`make_desktop.slim`）。裁的都是「打包后的 App 不会再碰」
  的东西，每一项在脚本里都写明了理由：

  | 项 | 量级 | 为什么能删 |
  | --- | --- | --- |
  | `bin/` 里 `python`、`python3.12` 两份 18 MB 副本 | 36 MB | 和 `python3` 是同一份二进制。换成指向它的**相对**符号链接：省下空间，`python` 也照旧能调，链接不写死路径、整个 App 挪动也没事 |
  | `python/lib/libpython3.12.dylib` | 17 MB | `bin/python3` 是静态链接的，`otool -L` 逐个验过：没有任何扩展模块引用它 |
  | `lib/PyObjCTest` | 16 MB | pyobjc-core 的测试包 |
  | 运行时自带的 pip / setuptools / ensurepip | 10 MB | 装好的 App 不会再装包（打包用的那份 pip 在裁剪之前就用完了） |
  | tcl/tk + tkinter / idlelib / lib2to3 / pydoc_data / venv / 标准库 test | 15 MB | 界面走 WebView，这些一个都用不到 |
  | `include/`、`config-3.12-darwin/`、`lib/bin` 里的脚本 | 4 MB | 不再编译扩展，启动器也不走那些脚本 |

  一条底线写在 `slim` 的 docstring 里：**裁完必须真跑一次**。`smoke_check` 只验证 import，
  而有些东西要到运行时才加载（WebView 后端就是开窗口那一刻才 import 具体的 framework
  模块）。裁剪后的完整首启实测：19 条出厂提示词 + 2 个技能播种、53 个静态资源与 9 次
  API 请求全 200、关窗后端口释放且无残留。

  顺带修了体积统计口径：符号链接原先按**目标文件**的大小重复计算（101 MB 被报成
  135 MB）。

- **打包成 macOS App**（`scripts/make_desktop.py` → `release/Quill.app`）。

  三件只有真打过一次包才知道的事，写进了脚本里，也是这一版的主要成本：

  | 事 | 为什么 |
  | --- | --- |
  | 依赖树用 `pip install --target` 平铺，**不建 venv** | venv 会把绝对路径写进 `pyvenv.cfg` 与 shebang，目录搬进 `.app` 之后就碎了 |
  | 运行时先找**本机 uv 装过的那份**再考虑下载 | uv 下的就是 python-build-standalone 的 `install_only` 包，同一个东西；而 GitHub releases 实测被限到几十 KB/s，那一趟能耗十几分钟 |
  | 装的是 `.[desktop]`，并且**打包后立刻做一次导入自检** | 壳自己在可选依赖里，少写 extra 的话打出来的包要等用户双击才报「需要 pywebview」；自检用随包的运行时导入关键模块，缺包、路径摆错当场暴露 |

  实测（模拟 Finder 双击：cwd 为 `/`、App 自己起服务）：数据根落在
  `~/Library/Application Support/Quill`、19 条出厂提示词与 2 个技能被播种、窗口里
  加载完整前端（53 个静态资源 + 8 个 API 全部 200）、关窗后端口释放且无残留进程。

### 修复

- **wheel 里没有出厂资源**（`prompt/`、`skills/`）：`bootstrap.template_dir` 最先找
  `quill_agent/resources/<name>`，而 pyproject 从没把这两样打进 wheel —— 源码树里跑
  一直正常（那条路退到「仓库根/<name>」），**装出来的包才会露馅**，而且表现是「新用户
  第一次打开，提示词和技能都是空的」，不报任何错。现在用 `force-include` 随包发布，
  打包脚本也会在缺失时直接失败。

  这个坑是打包过程里撞出来的：第一版脚本靠在装完依赖后「再复制一次」出厂资源，而当时
  后台残留的一个 pip 进程把 `lib/quill_agent` 重装了一遍，顺手抹掉了刚复制进去的东西 ——
  **打包日志一切正常，问题要等用户第一次打开才发现**。根治办法不是「复制得更晚一点」，
  而是让资源本来就属于那个包。

- **`--skip-deps` 从来没生效过**：依赖树原先直接装进 App，而 App 每次打包都重建，
  「跳过安装」自然也就没有东西可复用。现在依赖树装在构建目录（`build/desktop/lib`）再
  复制进 App。

### 变更

- **`quill serve` 的启动逻辑抽到 `server_runner.py`**，桌面壳与命令行共用同一份：
  挑端口、非本机监听的令牌校验、跑 uvicorn —— 各写一份必然漂移，而漂移出来的差异只在
  打包之后才暴露（端口顺延漏一个入口，双击启动那天就变成「打不开」）。

  `start_server` 因此**不阻塞**（阻塞式写法在壳里拿不到端口，只能去猜），命令行用
  `wait()` 补回原来的行为。`cli.pick_port` 保留为转发：测试与排障仍然从 `cli` 进来找它，
  而只跑 `quill -t` 的人不必为此付一次 Web 栈的导入开销。

### 内部

- `tests/test_server_runner.py`：四条用例 —— 非本机监听必须给令牌、端口顺延、起→应答→
  停整条链路（含端口放开）、服务自己死掉时**立刻**放弃而不是等满超时。
- `.gitignore` 排除 `desktop.log`（和 `checkpoints/` 同性质：本地运行产物，就地运行时
  就落在仓库根）。

## [0.1.19] - 2026-09-22

### 变更

- **数据根改到各平台的用户数据目录**（`config._platform_data_home`）：macOS 是
  `~/Library/Application Support/Quill`，Windows 是 `%APPDATA%\Quill`，Linux 是
  `$XDG_DATA_HOME/quill`。

  这是桌面版的第一块地基：安装目录只读，数据必须落在用户目录里；而主目录下的
  `~/.quill` 也不是各平台备份 / 迁移 / 卸载时会去看的位置。

  老数据（`~/.quill`）首次在新位置启动时**自动复制过去**（`bootstrap.migrate_legacy_home`）：
  只补不覆盖、旧目录保留 —— 不搬的话，老用户升级后看到的是「配置、会话、记忆全没了」，
  而它们还好端端躺在原地。三条约束缺一不可：只在用的是平台目录时才搬（`QUILL_HOME`
  与就地运行是明确指定的，不该从别处挪东西进来）、新位置还没有 `data/` 时才搬
  （两边合并只会让会话和记忆搅成一锅）、旧目录不删（回退版本时那边还是完整的一份）。

### 修复

- **「就地运行」的判据从「有 `data/` 或 `prompt/`」收窄成「像不像源码树 / 发布包」，
  并加上可写性检查**。旧描述有个隐蔽的后果：桌面版安装目录里同样带着源码
  （`pyproject.toml` + `src/` 都被打包带进去），而它在 macOS 的签名包内、Windows 的
  Program Files 下**都不可写** —— 数据会往只读目录里写，Windows 上还会被静默重定向到
  VirtualStore，用户和程序都看不出问题。旧判据另有一处别扭：首次运行时那两个目录还不
  存在，于是同一个仓库会先落到平台目录、第二次才就地。

- **`work_dir` 默认值不再无条件取 cwd**：cwd 是文件系统根（macOS 双击 App 时 Finder
  给的就是 `/`）或不可写时，退到 `~/Quill`。工作目录同时是文件工具的安全边界，取到
  `/` 等于边界形同虚设。

- 启动自检里的沙箱说明改为按**实际生效的** `settings.work_dir` 描述（原来自己读 cwd）。
  上面那条回退一旦发生，按 cwd 报出来的边界就是错的。

### 内部

- `.env` 的两个候选写成绝对路径：相对路径按 cwd 解析，而双击启动时 cwd 不可控 ——
  「换个启动方式就换了配置文件」这种问题排查起来极难。
- `tests/test_config.py` 新增「只读源码树不用作数据根」「cwd 不可用时回退」两条；
  `tests/test_bootstrap.py` 新增四条迁移用例（复制、显式 home 不搬、新位置在用时不合并、
  没有老数据时无副作用）。

## [0.1.18] - 2026-09-22

### 变更

- **正文和工具调用按发生顺序穿插显示**，不再是「工具堆一块、正文堆一块」。

  根因不在渲染，在**数据模型**：一轮回答存的是两个平行字段 —— `content`（说过的所有话
  拼成一整块）和 `steps`（调过的所有工具排成一个数组）。两个字段各有消费方（下一轮组装
  上下文用前者，审阅用后者），但代价是**丢掉了它们的相对顺序**。界面拿不到「哪句话在
  哪个工具前面」，所以不是没做穿插渲染，是**渲染不出来**。

  别的 agent 看起来是穿插的，是因为它们记的是**有序序列**，要「整块文本」时再拼出来 ——
  正好相反。现在后端在流式过程中**额外**记一份 `parts`：

  ```json
  [
    {"type": "text", "content": "先看看代码结构。"},
    {"type": "tool", "step": {"name": "list_dir"}},
    {"type": "text", "content": "看到了，接着改一下。"},
    {"type": "tool", "step": {"name": "write_file"}},
    {"type": "text", "content": "都改好了。"}
  ]
  ```

  **`content` / `steps` 照旧保留**（上下文组装、导出、审阅都还在用），前端**有 `parts`
  就按它渲染，没有就回退**到「思考 → 工具 → 正文」—— 那正是老消息当年存下来的样子，
  所以已有会话不会变样。

  三个实现细节：

  | 细节 | 为什么 |
  | --- | --- |
  | **同类分片合段** | 正文是逐分片到达的（一次回答能来几百个），每个分片一段会把界面切得粉碎。分段边界是「类型变了」或「工具到了」 |
  | **思考也在序列里** | 按它真实发生的位置 —— 它本来就夹在两次工具调用之间 |
  | **空段不产生条目** | 连着两次工具调用、中间没说话，不该在中间插一个空折叠块 |

  界面上：正文不折叠（它就是结论），思考与工具**默认折叠**（它们的价值是「看得见发生过
  什么」，要细看再展开 —— 一次长任务里这两样能占满整个屏幕）。折叠块也从「一个容器装
  多项」改成了**各占一块**：穿插必然要求它们彼此独立，中间要能塞进正文。

  实测（假模型刻意让正文和工具交替出现）：

  | | 渲染出来的元素顺序 |
  | --- | --- |
  | 改前 | 折叠 list_dir、折叠 write_file、正文（三段话挤在最后） |
  | 改后 | 正文 → 折叠 list_dir → 正文 → 折叠 write_file → 正文 |

  实时渲染和刷新后（读落盘数据）**两条路都验过** —— 它们走的是不同的代码路径
  （前端 store 累积 vs 读历史）。

## [0.1.17] - 2026-09-22

### 变更

- **两个子代理工具合并成一个 `spawn_agents`**（`tools/subagent.py`）：一次能派多个**并行**
  跑，只派一个就传一个元素。

  原本是 `spawn_agent`（一个）+ `spawn_agents`（多个）。用真实模型跑了 4 个任务 × 3 个模型：

  | 任务 | 特征 | 模型的选择 |
  | --- | --- | --- |
  | 通读整个包 | 一件大事、**可拆** | `spawn_agents` ×4（**自己拆的**） |
  | 三件独立的事 | 三件**互不依赖** | `spawn_agents` ×3 |
  | 一条有依赖的链 | **有依赖** | 不用子代理，自己串行 20+ 次 |
  | 读一个文件 | 单件小事 | 不用子代理，自己读 |

  **「该不该用子代理」它分得很清**（有依赖的不并行、小任务自己干），但**「用单个还是并行」
  它不分** —— 12 次尝试里 `spawn_agent` 被用了 **0 次**。而复数版传一个任务就是单个版，
  留着它只是白占 schema、多一个要选的东西。

  并行本身的代价全在**线程边界**上：子代理跑在线程池的工作线程里，而运行环境、交互通道、
  改动记录器三样都走 `ContextVar`，ContextVar **不跨线程继承**。少绑一样就是一个具体的
  故障 —— 子代理直接回「不在一次运行里」、看板一片空白、或者它改过的文件悄悄不进「还原」
  快照。账本则由父级收齐后**统一**合并（各线程自己 merge 是读-改-写，并发下会丢）。

  **两层上限**：`MAX_PARALLEL_AGENTS`（4，和 Ollama 默认并行槽对齐）与
  `MAX_SUBAGENT_TASKS`（8）。超出的在池子里排队而不是被丢掉 —— 拒掉的话模型会换个法子
  重试一遍，反而更贵。

  前端配套：事件带上 `agent` 标签（任务开头几个字），看板标题改成「N 个子代理正在工作」。
  实测（假模型，每个子代理停 3 秒）：两个子代理**同一秒开始、同一秒结束**，这一次工具调用
  总共 3.4 秒（串行会是 6 秒）。

### 修复

- **派子代理从来没弹过确认**。工具组的 `confirm`（需要用户点头的名单）里写的是
  `spawn_agent`，而模型调的是 `spawn_agents` —— 名字对不上，那条规则永远命中不了。设计
  意图是「另外花钱、要等，值得打断一次」，实际是静默失效、还没有任何提示。

  改名之外还加了**启动迁移**（`bootstrap.migrate_tool_names`）：`confirm` 是**存下来的**，
  出厂播种不会回头改老数据，不迁的话已有用户的名单里会一直留着一个不存在的工具名。

  实测：迁移前发一轮，事件是 `['start','round','tool_start']`（直接开跑）；迁移后是
  `['start','round','tool_start','question']`（停下来等确认）。

- **子代理看板从来没显示过**（前端，一直存在）。事件确实到了浏览器（在浏览器里抓过 SSE
  确认），但界面不更新：`run.subagentEvents.push(...)` 是**原地改数组**，而 `syncRunState`
  是靠**赋值**把状态同步到界面的，喂同一个引用时 Vue 的 `hasChanged` 判定「没变」、不触发
  更新。改成换一个新数组。其余实时字段没这个问题 —— 它们每次都是整个换掉，引用一定会变。

## [0.1.16] - 2026-09-22

### 新增

- **跑到一半的对话也落在会话文件里**（`chat._AnswerSaver` + `ConversationStore.upsert_run`）。

  在此之前助手消息只在整轮结束时写一次，所以「跑到一半被重启」时盘上只剩用户那句提问 ——
  想过什么、调过哪些工具、改了哪些文件，重启后全看不出来。现在边跑边写。用假模型（第二轮
  请求挂住不返回）+ `kill -9` 实测：那一轮**还在跑**的时候，盘上已经有正文和工具步骤；
  重启后从接口取历史，那条 `partial: true` 的记录原样还在。

  两件事得同时成立才算做对，所以两件都测：

  | 要求 | 做法 |
  | --- | --- |
  | 盘上**一直有** | 每来一个事件问一次要不要写，而不是攒到最后写一次 |
  | 盘上**只有一条** | 按 `run_id` 找到这一轮那条**替换**，不是 `append`（写十次就是十条重复消息） |

  两个例外都是实测逼出来的，不是预先想出来的：

  - **空快照不写**。事件的时间线常常是「几十毫秒内到齐 → 长时间静默（等模型、等工具）」，
    而第一个事件是 `round`——那时还没有正文。让它占掉「首次写」的机会，后面的正文和步骤
    就全被节流挡掉；挡完之后没有新事件来推动下一次落盘，盘上便永远停在一个空壳上。
    实测看到的就是一条**内容为空**的消息。
  - **工具步骤强制写**。它恰好出现在那串密集事件的中段，不强制就会被节流吃掉 ——
    盘上就只剩「它说了什么」，没有「它做了什么」。

  落盘失败分两种对待：中间态吞掉（附加的东西，一次 IO 抖动不该把整轮对话打断），
  收尾那次往上抛 —— 那一刻这一轮的结果真没留下来，异常会变成一条 Notice 让用户知道。

## [0.1.15] - 2026-09-22

### 修复

- **改动快照改为「改动一发生就落盘」；在此之前，跑到一半重启就再也还原不回去**（`changes.py`）。

  原先快照是整轮结束时写一次（`stream_round` 收尾那句 `save`），于是有一个窗口：**文件已经
  改了，而还原要的前像还在内存里**。这期间进程重启或断电，文件就永远停在改过之后的样子 ——
  而窗口随任务变长而变大，长任务恰恰最需要能退。用假模型（工具跑完后挂住不结束）+ `kill -9`
  实测复现过：

  | | 改前 | 改后 |
  | --- | --- | --- |
  | 断电前的检查点 | 目录根本不存在 | manifest + 前像已落盘 |
  | 断电后的还原 | `{"restored": []}` —— 退不回去 | `{"restored": ["l2-target.txt"]}` |

  实现是把 `save` 拆成可反复调用的 `_flush`（幂等），`record_text` 每记一笔调一次。两个配套
  的东西不能省：

  - **manifest 改原子写**（临时文件 + `os.replace`）。写入从「整轮一次」变成「每笔一次」，中途
    被杀的概率随之变高，而一个截断的 manifest 会让这一轮永远退不回去 —— 恰好是这层要防的事。
  - **落盘失败不抛**。文件已经改了，丢的只是还原能力；为此让工具调用失败是拿小毛病换大毛病。
    但服务端日志会留一行，否则用户会发现「还原」悄悄不管用了、却不知道是从什么时候开始的。

  `restore` 顺带容忍读不出来的 manifest（返回空，而不是让按钮直接报错）。

## [0.1.14] - 2026-09-22

### 新增

- **MCP 接入：把外部服务器提供的工具变成可用工具**（`mcp.py`）。

  这是「工具的来源」第一次不只来自本仓库的代码 —— 内置工具在 import 时静态注册，而 MCP
  工具要**连上之后**才知道有什么。项目早就为这一天留了接缝（`ToolKind` 的注释里写着
  「将来接 MCP 时在这里加成员，注册表和页面都不用改结构」），这次用上了：

  | 位置 | 改动 |
  | --- | --- |
  | `ToolKind` | 加 `MCP` |
  | `ToolRegistry` | 加**运行时**注册 / 按来源整批注销（`register_external` / `unregister_source`） |
  | `resolve_mode` | 工具组里的 `mcp:<server_id>` 展开成该服务器**当前**的工具 |
  | `mcp.py` | 每个服务器一个后台事件循环线程 + 同步调用桥 |

  **为什么工具组按「整个服务器」引入，不逐个勾选**：服务器上有什么工具由它自己决定，而且
  它升级之后会变。逐个勾选的名单不会自动跟上 —— 新工具静默不可用，而用户完全不知道为什么。
  「我要引入这个服务器」也正是用户的心智模型。

- **MCP 设置页**（偏好设置 → MCP）：增删改查、启停、**保存前试连**。

  试连那一步是关键：用户填的是一行命令加一串参数，光看配置判断不了它能不能跑起来（命令
  不存在、包名写错、要联网、要 token）。试连让他当场看到「能连上，它提供这 3 个工具」，
  而不是保存之后对着一个空工具列表猜。试连用的是**一次性连接**，用完即断。

  工具组编辑里也加了「引入 MCP 服务器」的勾选（在逐个勾选工具**之前**，因为它是更粗的粒度）。

- **安全上写清楚的一条**：添加 MCP 服务器 = 允许它在这台机器上执行任意代码。stdio 会起一个
  进程，而且**它在沙箱之外** —— 沙箱管的是模型执行的命令。界面上明确写出了这一点，配置默认
  为空，且 `McpServer` 没有 `builtin` 那一套（不存在「出厂自带的服务器」，那等于我们替用户
  决定这台机器上要跑哪些外部进程）。

- 依赖加 `mcp`（官方 SDK，2.2.0）。**不自己实现协议**：MCP 还在快速演进（传输形态、版本
  协商、认证都在变），自己实现等于给自己接一个持续追踪的活。

### 修复

- **工具组保存时把 `mcp:<id>` 当成拼错的工具名拒掉**。用户看到的是「未知的工具名：
  mcp:574b4…」，而那个引用其实是对的 —— 校验只查了注册表，没认出「这是对服务器的引用，
  展开在别处」。现在放行引用、但要求那个**服务器确实存在**（否则还是「引用了一个不存在
  的东西」，只是换了个对象）。

  **这是端到端实测抓出来的**：后端接口本身没问题，是在浏览器里点一次「创建」才撞上的。
  同时补了三条测试钉住它（放行合法引用、拦掉不存在的服务器、「需要确认」不接受服务器引用）。

- 「需要确认」不接受 `mcp:<id>`：它展开成一批**名字不同**的工具，那条规则永远命中不了 ——
  与其静默失效，不如要求逐个指定（那批工具在工具列表里可见、可勾）。

### 内部

- `tests/test_mcp.py`（13 条）+ `tests/fake_mcp.py`（测试用的最小 MCP 服务器，**自己写**而
  不连第三方：测试不该依赖网络、npm 或别人的服务是否在线）。
- 三处只有真实进程才能验证的行为被钉住了：工具自己报错时**连接不能坏**（返回错误文本）、
  连不上的服务器只影响它自己、断开后调用给一句能看懂的话。

## [0.1.13] - 2026-09-22

### 新增

- **改动记录器：一轮里改了哪些文件，并且能一键还原**（`changes.py`）。

  在此之前，模型改完文件，用户只能看到工具步骤里一行「edit_file 3ms」—— 不知道改了哪几行，
  也没法回退。而 `delete_file` 的工具描述里自己写着「这是不可撤销的操作」。**审批做得很严，
  审阅却做不了**，这是能力结构上的一个缺口，这次补上。

  机制是**懒快照**：文件工具在动手**之前**调一次 `changes.record_text()`，把原内容记进当前
  这一轮的记录器。记录器用 `ContextVar` 送到工具里（和 `interaction` 同一套办法）—— 工具签名
  不用加参数，没激活时静默跳过，工具本身照常工作。跑完按内容 hash 落盘到
  `checkpoints/<会话>/<run_id>/`；同一个文件被改多次只存**最早**那份（还原要的正是它）。

  挂点：`write_file` / `edit_file` / `delete_file` / `move_file`（源和目标各记一条）/
  `copy_file`。**`make_dir` 不记** —— 空目录不是「改动成果」，而还原时删目录有误伤非空目录
  的风险。

- **改动可视化：折叠标题带 `+12 −3`，展开看 diff**。

  统计放在**标题上**而不是只放在折叠里：扫一眼就知道改了多少，是这件事的主要价值；想看具体
  哪几行再展开。diff 由后端用 `difflib.unified_diff` 算（改动前的原文只在后端有），截到 200 行，
  **不回灌给模型** —— 那是给人看的，回灌等于每轮白烧一倍上下文。

  **新建文件的 diff 有意留空**：那份「改动」就是全文，全绿的一片没有信息量，只报行数。

- **消息下面一行「✎ 改了 N 个文件」+ 还原入口**。

  确认框里写明**命令的副作用不在还原范围内**：模型跑 `sed -i`、`mv`、`git checkout` 改掉的
  东西追踪不到，不说的话用户会以为「还原」等于「什么都没发生」。二进制和过大的文件只记
  「改过」，还原时如实报告跳过（存了也还原不回去）。

- **git 工具**（`tools/git.py`）：`git_status` / `git_diff` / `git_log` / `git_commit`。

  不让模型自己敲 `run_command git ...`，有三个具体理由：`git status --short` 的状态码位置是
  有含义的，小模型经常读反（把「工作区改了」看成「已暂存」，然后提交出一份空的还报成功）；
  **commit 是不可逆的对外动作**，而 `run_command` 那条确认只认灾难性命令；`git diff` 稍大就是
  几百行。所以 commit 走确认通道，diff 截断。**只做本地的事，不 push** —— 提交是自己仓库里的
  事，推出去是给别人看的。

- **符号检索**（`tools/symbols.py`）：`outline` / `find_symbol`。

  `outline` 是最容易见效的那个：800 行的文件整读要上万 token，骨架只要几十行，看清结构再决定
  读哪一段。`find_symbol` 找定义和引用的位置，比 `search_content` 准（只认定义行的写法）。

  **实现是正则启发式，不是 LSP / tree-sitter**：那两条路都更准，但一个要为每种语言起 server，
  一个要随包带 C 扩展的 grammar，都会破掉「clone 下来 `make api` 就能跑」。工具描述和返回值里
  都写明「这是候选，拿不准要看那几行」；将来换实现，签名和工具名都不动，模型侧不用改。

- **每条消息都能复制和删除**（用户说的和模型说的都有）。

  复制拿走正文；正文为空时退回思考过程 —— 只有工具调用的那一轮，思维链才是想留下的东西。

  删除是**真删**（`history.delete_at`）：从会话文件里去掉那条记录，下一轮组装上下文不会再
  带上它。只从界面上藏起来是最糟的做法 —— 用户以为删掉了，模型还记得。

  两个图标放在**消息下面那一行的右侧**（同一行左边是耗时与用量），常驻但很淡、悬停只提亮、
  **不加底色** —— 加底色会让消息流里凭空多出两个方块。为此显式压掉了 Element 按钮自带的
  `--el-button-hover-bg-color`。

  三处判据上的取舍：

  - **正在生成的那条不给按钮**：它还没进服务端历史，按索引删会删到别的一条。
  - **判据是「列表最后一条 + 此刻正在跑」，不是看有没有 `ts`**：刚发出去的用户消息是前端
    就地 push 的，那时它也没有 `ts`。按 `ts` 判断会让用户消息一直少两个按钮 —— **这个坑是
    实测才发现的**（4 条消息只出现了 2 组按钮，而日志里明明有 ts）。
  - **摘要那条不给删除**：它是压缩的产物，删掉它，被它覆盖的原文下一轮会被重新塞回上下文，
    等于什么都没做。

  删完之后**重新拉一遍消息列表**，不就地改本地数组：删掉一条，后面每条的索引都往前挪一位，
  而删除按钮正是按索引传给后端的 —— 就地改很容易漏掉这一点，下一次点删除就删错一条。

### 修复

- **改动还原的端点和「从归档恢复会话」撞了路径**：`POST /conversations/{id}/restore` 两边同名，
  先注册的那个赢了 —— 于是「还原文件」的请求静默地变成了「恢复会话」，返回一个
  `{"restored": false}`，用户看到的是「点了没反应」。改到 `/changes/restore`：两者语义本来就
  不同，挤在同一路径上迟早有人调错。

  **这是端到端实测抓出来的**：单测覆盖不到路由注册顺序，只有真发一个 HTTP 请求才看得见。

### 内部

- `tests/test_changes.py`（12 条）：改动行计数、diff 截断、新建不出 diff、保存/还原往返、
  同一文件多次改动取最早的 before、**路径穿越防护**（manifest 是磁盘文件，不能成为跳板）、
  二进制只记不还原、会话被删时清检查点。
- `tests/test_code_tools.py`（13 条）：git 各工具、非仓库时的友好提示、**用户拒绝确认时仓库
  一动不动**（这是确认通道存在的全部意义）、符号检索的词边界（搜 `read` 不该匹配 `read_file`）
  与生成物目录跳过。

## [0.1.12] - 2026-09-22

### 新增

- **出厂就带三个能直接用上的模式**（`defaults.py`）。新装一份应用时数据是空的，而任务页
  **必须选中一个模式**才能对话 —— 也就是说，不预置的话，「装好」和「能用」之间隔着一整套
  配置的搭建：建六条提示词、拼成组、从 27 个工具里挑、最后组装成模式。

  | 模式 | 提示词组 | 工具组 | 技能组 | 记忆 |
  | --- | --- | --- | --- | --- |
  | 全量 Agent | 全量提示词组（六类各一条） | 全量工具组（注册表里全部工具） | 全量技能组 | 开 |
  | 轻量 Agent | 轻量提示词组（六类各一条、正文极简） | 轻量工具组（六个核心文件工具） | 空 | 关 |
  | 仅问答 | 空 | 空 | 空 | 关 |

  全量提示词组那六条是按**项目当前的样子**写的 —— 沙箱、审批、子代理、待办清单、技能与
  记忆都写了进去，不是一套换个项目也能用的通用模板。

  「全量工具组」的工具清单**从注册表现取**，不手抄一份：抄的话，以后每加一个工具它就
  悄悄少一个，而它叫「全量」，用户没有任何理由去核对。

  **「轻量 Agent」给上下文只有 8K/12K 的小模型**。小窗口最怕的是「每次请求都带」的东西：
  全量六条提示词一千多 token、27 个工具的 schema 好几千 —— 所以轻量版提示词压到两百字
  上下（六类的骨架不变），工具只留列目录 / 搜索 / 读 / 写 / 改 / 问用户六个，技能与记忆
  一并关掉。工具清单这里**必须手抄**（与全量相反），测试两头把守：每个名字都要在注册表里
  真实存在，而且数量必须真的比全量少。

  三个模式的偏好模型都留空（= 沿用任务页当前选中的模型）：出厂时不知道用户配了哪个模型，
  硬指一个只会让默认模式一装上就是坏的。

- **四类数据加了出厂标记**（`builtin`）：模式、提示词组、工具组、提示词。出厂资源的这个
  值是 `True` —— 界面上**干脆不显示删除按钮**（只留一个「内置」标签说明原因），
  **存储层也会拒绝**。

  **删除拦掉、编辑照旧留着**：改一处措辞、调一下描述都是正当需求，而且改完出厂那份内容
  也不会消失；「删掉」则会让「开箱即用」永久少一块 —— 出厂的那个模式正引用着这些组和
  提示词。

  两层都要，因为它们挡的不是同一件事：只有界面的话，一次误调接口就能删掉，而用户要等到
  下次启动、发现模式引用的组不见了才知道；只有存储层的话，用户点下去只会拿到一句 400 的
  文案 —— 那是失败的体验，不是保护。

  标记不由界面写入（请求体里没有这个字段），播种时还会**把被抹掉的标记改回来** ——
  少了它，一条出厂资源就成了「用户可以删掉」的，而那恰好是这个字段要防的事。

### 变更

- **提示词库的类别筛选改成六个等高按钮**，紧跟「提示词库」标题、默认停在第一类。原来是一个
  下拉框：要点开、看清、再选，三步；而类别总共只有六个、还是固定不变的六类 —— 铺开成
  按钮一步就到，也顺带让人一眼看完提示词是按哪六类组织的。按钮高度对齐
  `--el-component-size-small`，跟同排的名称过滤框一样高 —— 不对齐的话六个挨着输入框排，
  会显矮一截。

- **任务功能区的间距改成均匀的一排，左右都顶到边界**（`justify-content: space-between`）。
  原先「思考开关 + 用量仪表」被 `margin-left: auto` 推到最右，中间空出一大块，整排读起来
  是「左边一堆、右边一堆」；现在所有项等距排开，第一项贴左、最后一项贴右 —— 均分也要求
  每一项都是平级的兄弟，所以「思考 + 仪表」那个包一组的写法一并去掉了。

- **偏好设置加了一层页内二级导航**（顶边栏正下方）：主题 / 对话 / 联网搜索 / 关于各占
  一个地址。原先它们挤在同一页里，体量差得很远 ——「联网搜索」自己就是一整页的配置
  （后端、Key、端点、试搜），跟「主题」并排只会互相埋掉。

  做过一版侧边栏二级菜单，放弃了：侧边栏要先点开父级才看得到几节，而这几节本来就互相
  独立，每次换一节都得「展开 → 再点」两步；摊在页面顶部是一眼可见、一次点击。侧边栏
  留给一级项，**联网搜索也归到了偏好设置下面**（`/search` 旧地址留了跳转）。
  滚动只发生在内容区，二级导航固定不动 —— 它是「我在哪一节」的路标。

- **功能区在「思考」与「缓存」之间加了一道分隔线**：左边是「我选的」（设置），
  右边是「跑出来的」（数据）。

- **「正在生成…」「等待你的回答…」这些阶段提示挪到顶栏**，跟在会话名后面。
  原先它压在输入框正上方：功能区那一排是每轮选择的东西，夹进一行随运行出现消失的
  状态字，观感上添堵；而「是不是卡住了」的答案本来就属于视线常驻的地方 —— 顶栏一眼
  可见，用时与轮次照旧跟在后面。

- **点「停止」现在能立刻掐断正在生成的回答**。原先取消信号只在工具轮次之间被检查，
  纯输出的一轮要等模型把话说完（几千字的收尾实测拖了十几秒）才收场；现在流式解析的
  **每个 chunk 上都有检查点**，发现取消就主动断开上游连接、丢弃半成品 —— 按下停止
  到界面收场降到两秒以内。半截回复同样不进历史，和「轮与轮之间取消」同一个口径。

- **功能区的四项设置改成「标签在前、控件在后」**：模式 / 模型 / 工作目录 / 附件各自前面
  加两个字，标签和控件包在同一个 `.tool-group` 里（跟着整排均分、换行也不拆散）。
  光一个下拉框立在那里，看不出它管的是哪件事。附件按钮因此只剩一个加号 —— 标签已经
  说了这一格是什么，按钮里再写一遍「附件」是重复；模式、模型两个选择框顺势收窄
  （150 → 120、220 → 180）：有标签在旁边之后，宽度只需要装得下名字。

- **上下文用量去掉占比圆环**，只留 `48k/128k` 这行读数：占多少百分比改由悬停提示给出
  （`上下文占用 37%（48k / 128k tokens）`）—— 那是个确切的数，比一个没有刻度的圈准。
  圆环横在「缓存」和「上下文」两组读数中间，把它们撑开 56px，而整排功能区的间距是
  均分的（54px），唯独这两处读数被一个 40px 的圈隔开，看着是三组而不是一组。

- **「缓存」和「上下文」两处读数改成跟着整排一起均分**（`.meter` 在窗口够宽时用
  `display: contents`，自己不生成盒子，两处读数直接成为 `.tools` 的平级项）。
  原先它们被包在同一个 flex 容器里，组内间距是写死的 8px，跟外面 `space-between`
  均分出来的 48px 对不上 —— 结果是「缓存」左边 48px、右边 8px，看着往右偏。
  现在两边一样宽（实测 1280 窗口下都是 48px）。窗口窄到整排要换行时不拆
  （`@media (min-width: 1100px)`）：拆开的话换行会把这两处甩到同一行的两端，
  实测 900 宽时中间空 595px，比挨在一起难读得多。

- **「导出」从顶栏挪到侧栏每个会话行上**（归档按钮左边），只剩一个图标。
  在顶栏时它只能导出当前这一条，而「把某次对话拿走」对每一条都成立；挪进列表之后
  一行里并排两个动作，再写「导出」两个字会把标题挤没。实现上仍是 `<a download>`
  （`el-button` 的 `tag="a"`）—— 进度、保存对话框、大文件都是浏览器自己的事。

- **侧栏的滚动收进会话列表**：新建任务与导航不再跟着滚。原先滚的是整列，
  往下翻几屏之后导航就跑到可视区外，想换个页面还得先滚回去。现在窗口再矮也只压
  会话列表（`flex: 1` + `min-height: 0`），导航与新建按钮原地不动。

- **运行状态提示（等待模型响应 / 深度思考中）挪到顶栏正中**。原来贴在会话名后面，
  而它是随运行出现、结束消失的东西 —— 参与排布的话每次出现都把标题往右挤一下、
  消失再弹回来。改成绝对定位浮在顶栏中间那一层，标题一动不动（实测运行前后标题
  左边界都是 289px）。

- **「新建连接」拆成「官方服务」与「自定义连接」两个入口**。官方服务从一份
  服务商清单里挑一家（223 家，可搜索），名字、接口地址、协议自动带出，剩下的
  与自定义入口完全一样。两个入口**共用同一张表单、同一个提交**，差别只有要不要
  显示那格服务商选择器 —— 官方服务无非是替你填好三格，填完仍是一条普通连接。

  清单来自 models.dev，与模型规格快照是**同一次下载写出的两个文件**
  （同一份 api.json 里既有服务商也有模型），所以共用「同步模型库」一个按钮。

  一处容易踩空的地方：第一方官方（OpenAI、Anthropic、Groq、Google…）在快照里
  **没有 `api` 字段** —— SDK 自己知道官方地址，没必要再抄一遍。拿「有没有地址」
  当筛选条件，恰好会把最常用的几家全漏掉。所以地址留空是正常状态，本项目的
  `base_url` 留空本来就是这个意思（见 `Protocol.default_base_url`）。

- **板与板之间的缝不再像「接了一块别的东西」**：根因是模糊只做在板上，板内是糊开的
  背景、缝里却是没模糊的原始背景，边缘清晰度突变。现在模糊提到 `.shell` 一层、
  整窗只做一次，板只叠半透明白。

- **主按钮（新建任务、发送）从琥珀金渐变改成「冰雾高光」**：浅灰蓝渐变
  （`#EAF2F8 → #C8DCE8`）压深墨字，投影换成暗色。金色留给真正需要一点温度的地方
  （笔尖、行内代码）。发送按钮只在它是「发送」时用这套配色 —— 运行中那个位置换成
  的「停止」按钮保持默认样式。

- **实底金色全部换成「冰雾高光」**：会话行的选中块、用户自己的消息气泡、深色模式下的
  主按钮（提示词 / 工具 / 模式等页的「新建 / 添加」与弹窗确认），以及深色模式下的
  警告提示条。取色收在 `--ice-bg` / `--ice-text` / `--ice-shadow`（两套主题各一份），
  改一处全站生效。深色那份把不透明度收了一档 —— 全亮的浅蓝块在深靛蓝界面里会亮成
  一块灯箱。金色只留小面积（笔尖、行内代码）。

- **顶栏改成「会话名居中、状态在名字下面一行」**。名字回答「在跑哪个任务」，
  状态回答「跑到哪一步了」，上下两行一次看全；两者都随会话切换、随运行出现消失，
  所以整格绝对定位浮在顶栏中间那一层，左边的「任务」不会被推来推去。

- **深色模式表格的隔行底色不再是黑的**。Element 取的是 `--el-fill-color-lighter`，
  深色主题下它是纯黑灰（`#1d1d1d`）—— 压在玻璃板上就是一道道黑条，板子的通透感
  全没了。换成板内那一档半透明底，跟着主题走。

- **各页面顶栏的说明文字精简**（提示词 / 工具 / 技能 / 模式 / 记忆 / 模型 / 用量 /
  归档）：原来是一段话，现在是「这页是什么 + 什么规则决定它」，跟着标题一眼扫完。

- **顶栏的会话名调大一档**（12px → 14px），宽度上限从固定的 62% 改成
  `min(70%, calc(100% - 320px))`：字号上去了，能显示的字数也比原先多 —— 那是顶栏
  中间的主角，说明文字才是配角。

- **消息的「复制 / 删除」挪到正文之后、任务清单之前**。原先它在整条消息的最末尾，
  一轮长任务里会被任务清单和工具折叠块顶到屏幕外，想复制那句结论得先跨过一整屏的
  过程记录。现在紧跟正文，再往下一行才是清单。

- **会话行里「导出」与「归档」贴在一起**。Element 默认给相邻按钮留 12px 外边距，
  两个 24px 的图标按钮之间因此空出一道比按钮还宽的沟；间距改交给 `.conv` 的 gap。

- **归档从侧栏一级项收进「偏好设置」**（二级导航里排在 MCP 之后）。旧地址
  `/archived` 留了跳转，书签和历史里的链接不会落到空白页；e2e 补了一条钉住它。

- **用量折线图不再画全零的模型**：一条贴底的直线不带任何信息，只会把图例撑长、
  把真正用过的几档颜色挤到边角。「配了但没用过」的模型仍能从下面的表格看到。

- **归档 / 记忆 / API 设置 / MCP 四页的滚动条收进列表里**。原先滚的是整页 ——
  往下翻几屏，标题、搜索框、按钮全跟着跑掉，想换个筛选条件得先滚回顶部。
  现在滚动发生在表格上，表头与操作行钉在原处（和提示词、工具、技能三页一致）。

- **顶栏右侧的执行权限按钮加了一层层级保护**：顶栏中间那一格是绝对定位的，万一
  某页标题很长把它撑过来，也不能挡住这个入口 —— 它点不开等于权限改不了。

- **深色模式的强调色从暖金换成冰雾蓝**（`--accent`：`#c79a4e` → `#a9cbe8`）。
  金色做**实底**高亮很好看，但当**文字**用（链接、编辑 / 删除、导航选中）就出问题：
  它亮度只有 0.36，压在玻璃板上的对比度约 2.2:1，用户反馈「看不清」；冰蓝约 3.6:1。
  金色没删，只是提亮一档（`#d9a04a` → `#e6c887`）、只留给小面积（行内代码、笔尖）。
  连带把单选按钮组选中态、复选框的勾改成压在主色上的深字（默认是白的，压浅蓝读不清）。

- **深色模式下按钮与开关的底色加深**（新增 `--btn-bg` / `--btn-bg-hover`，浅色那份
  与原来一致，只有深色那份从「半透明白」换成「半透明黑」）。半透明白底等于往玻璃上
  又糊了一层雾，彩色的字和图标（编辑 / 删除 / 权限三档 / 开关）全被冲淡；换成深底
  之后字才立住。文字按钮（编辑 / 删除）也给了这层底 —— 侧栏会话行里的两个小图标
  除外：那一行选中时整块是冰雾浅底，再叠深底会把图标埋掉。

- **弹窗内容过长时在弹窗内部滚**（全局给 `.el-dialog__body` 62vh、确认框 50vh）。
  原先 Element 让弹窗长到内容那么高，内容一超视口滚动条就落到页面上 —— 标题与底部
  按钮跟着滚走，看着像「弹窗被页面吃掉了」。使用说明里那层重复的 max-height 也撤了，
  免得套出两条滚动条。

- **前端产物的缓存策略**（`server/main.py`）：入口页 `no-cache`（每次校验），
  `assets/` 下带 hash 的文件 `immutable` 长缓存。原先两者都没有缓存头，浏览器会自己
  猜 —— 入口页一旦被缓存住，重新构建后它引用的旧 chunk 已经不在 dist 里，表现就是
  **「页面看着还在、点按钮没反应」**，正是用户报的那类症状。

- **文字按钮回到「无框无底」**（编辑 / 删除 / 复制 / 导出…），只有 hover 时才浮出
  一层淡玻璃底。上一轮给它们加的深色底观感太重 —— 一排小牌子盖在表格上，读起来像
  按钮组而不是行内动作（设计评审图同样是「无框无底」那版）。可读性改由字重解决：
  文字按钮 `font-weight: 600`，**纯图形按钮**给 SVG 描一圈同色边（Element 的图标是
  填充路径，本来没有描边，`stroke-width` 一加线条就粗了一档）。

- **弹窗宽度跟着视口收**（`max-width: calc(100vw - 32px)`，确认框同理）。各页写的都是
  固定宽度（640 / 760 / 560px…），窗口比它窄时弹窗会被挤出屏幕 —— 表现正是
  「点了按钮只看见背景变暗，弹窗本体在屏幕外」。实测 560px 窗口下原来的弹窗右边有
  93px 在视口外，修完三种窄窗口都完整落在屏内。

- **懒加载代码块加载失败时自动重载**（`vite:preloadError`）。页面开着的时候前端重新
  构建过，老页面引用的 chunk 已经被删 —— 用户一点就会落进一个「看着还在、点着没
  反应」的界面。现在整页自动刷新一次回到新版本（配合入口页的 `no-cache`，
  这类问题不会再拖着用户）。

- **深色下可读性的第二刀：撤销「压暗底色」，改成提亮 + 加粗**。
  上一轮给按钮压深底色确实让彩字更立，但整排按钮变成一块块深色牌子，观感太重 ——
  底色回到半透明白，可读性改由「字加粗 + 语义色提亮」负责：权限三档色整体提亮一档
  （`#e5825a` / `#6aa9dd` / `#6cbf8b` → `#ffa77d` / `#8ec4f0` / `#8fd8ab`）。

- **标签也一并处理**（模式页的「启用 / 关闭」、技能页的技能名、API 设置的模型名…）。
  根因是 Element 的两种效果都与底色同色系：`light` 是「主色淡化当底 + 主色当字」，
  底和字糊成一片；`plain` 是透明底 + 彩色字，彩色字直接压在玻璃板上。
  现在统一色相不动、文字亮度提一档 + 加粗。

- **弹窗去掉 `backdrop-filter`**。弹窗自己的底色本来就 94% 不透明，再叠一层背景模糊
  看不出差别，却会让它在某些显卡 / 驱动组合下**整个不绘制**（Chrome 合成层的老问题：
  模糊层算不出来就什么都不画）。用户报的正是这个症状 —— 点按钮后**整个页面变暗
  （遮罩在）、按 ESC 又恢复，但弹窗本体始终不在屏幕上**。下拉与气泡保留模糊。

- **弹窗终于找到了真凶：它没挂到 body 下**。
  四块玻璃板都有 `backdrop-filter`，而 `backdrop-filter` 会给元素建一个层叠上下文 ——
  `position: fixed` 的参考系因此从视口变成了那块板子。弹窗渲染在哪个组件里，就相对
  哪个板子定位：顶栏那个「执行权限」的弹窗被困在 72px 高的顶栏里（用户看到的
  「只有遮罩」），页面里的弹窗则在内容区居中（用户看到的「范围只有当前这个区块」）。
  顶栏的「使用说明」在侧栏品牌板里，同理。
  现在 11 个弹窗全部显式 `append-to-body`（工作目录选择器本来就有）：遮罩覆盖整屏、
  弹窗相对视口居中。

- **弹窗与确认框的底改成实底**。Element 默认让它取 `--el-bg-color`（我们映射成了
  `--bg-card`，深色下只有 11% 的白）—— 弹窗因此是半透明的，背后的正文直接透过来，
  和弹窗里的字糊成一片。改用 `--overlay-bg`（94% 不透明）。

- **深色下标签的黑边换成亮边**。`effect="plain"` 的边框取 `--el-color-*-light-5`，
  深色主题下那几个值是往黑里混的 —— 于是「记忆」「提示词」这些列的标签描了一圈黑边。
  现在统一是半透明白（各语义色则用自己色相的亮线）。

- **记忆页的「添加 / 清空」放大并居中**：改用默认尺寸（原来是小一档），与三行高的
  输入框垂直居中对齐 —— 贴顶会让它们显得比实际更小、位置也偏上。

- **用量页两块的高度定下来**：图表区封了 50% 的上限、图例最多两行（多出来在图例条内
  部滚），表格区保住 220px 的下限。原先图表区高度完全由内容撑 —— 模型一多，图例折成
  三四行，表格区就被挤得只剩表头（用户反馈「按任务统计的列表被挤没了」）。
  实测两块 320 / 306px，表格可视 6 行。

- **整页不再滚动，也不再「抖一下」**：`html, body` 加 `overflow: hidden` +
  `overscroll-behavior: none`，内部滚动容器加 `overscroll-behavior: contain`。
  前者挡住「内容比视口高一两个像素」时多出来的那点可滚空间（滚轮一碰整页就上下动）；
  后者关掉滚到边界后传给外层的弹性回弹（macOS 上那记橡皮筋）。滚动本身不受影响 ——
  它一直都发生在内部容器里。

- **折线图的高度改成随窗口变化**。原先画布写死 240px 高，面板一矮就被裁掉一截；
  现在 `LineChart` 里的 ResizeObserver 连**高度**一起量（图例以下那块区域），
  svg 按量出来的真实像素画 —— 窗口一高图就跟着高。用量页的两块也随之改成平分
  页面高度（各 `flex: 1 1 0`），表格区仍保 220px 下限。
  实测窗口 1050 / 800 / 620 高时，图表区分别是 343 / 218 / 128px，三档都不裁。

- **API Key 改为明文回显**。后端原先把它脱敏成 `••••••` 再回传，界面于是只能显示
  一串点、或者干脆留空 —— 两种都会让人以为 Key 丢了（连着的两轮反馈都是这个）。
  这是本地单机应用（默认只监听回环地址，见 `server/auth.py`），用户要看的就是自己
  填进去的那把钥匙，所以列表与增改接口都按原样回传。连带把为「不回显」加的那套
  东西（留空提示、已保存标记、按连接补 Key 的测试参数）一并撤掉。

- **弹窗不再整体滚动**（`.el-overlay-dialog { overflow: hidden }`，弹窗内容区按视口
  算高度 `max(160px, calc(100vh - 300px))`）。Element 默认让外面那层可滚 —— 弹窗比
  视口高时滚的是整个弹窗，标题栏和底部按钮一起滑出屏幕（用户反馈「API 设置的连接
  窗口还能相对窗口滚动」）。实测矮窗口（520px）下：弹窗内容区自己滚、外层不可滚、
  弹窗底部不超出视口。另外逐页确认了整页都不可滚（10 个页面 `scrollTop` 恒为 0）。

- **修复列表滚不动的问题**（API 设置 / 归档 / 记忆 / MCP 四页）。原先的做法是在表格
  外面套一层 `el-scrollbar`，但 el-table 内部的 `.el-table__body-wrapper` 是
  `overflow: hidden` 且高度等于全部内容 —— 鼠标停在表格上滚滚轮时，浏览器找不到
  可滚的祖先，表现就是「列表没反应」（用户反馈）。改成**表格自己滚**：量出容器高度
  交给 el-table 的 `:height`，表头固定、只有表体在滚（和用量页一直是同一套做法，
  现在抽成 `composables/useTableHeight.ts` 共用）。实测 16 条连接、520px 高的窗口下，
  滚轮能把表体滚到 426px；修复前恒为 0。

- **新增公共剪贴板**（页面右侧那道空隙里的一个小箭头，点开向左拉出面板）。应用内
  复制过的内容会自动收进去，按时间倒序、逐条铺开（不折叠），可以「再复制一次」或
  单条删除，最多留 50 条、落 localStorage。
  动机：复制往往是为了到别的页面去用，而系统剪贴板一次只装得下一份 —— 复制第二段
  就把第一段挤掉了。记录**不依赖系统剪贴板是否写成功**（后者可能被浏览器拒掉），
  那样至少还能从面板里取走。
  位置与形态：**整条右侧空隙都是触发区**（宽 16px —— 正是 `.shell` 的 padding 留出的
  那道缝，一像素也不压到任务区），从顶到底铺满；鼠标进到这条里任何位置，整条亮起来
  表示可点，点哪都能开合（只在那个小箭头上做高亮的话，得精确瞄中 16×46 的方块）。
  高亮是**从中间往上下两端渐隐**的（中间最实、两端化开）—— 一条均匀色带从顶贯到底
  太生硬，像贴了张纸条。用伪元素 + opacity 过渡实现：渐变本身没法做过渡动画。
  默认无底无框、箭头也是淡色。面板从它**左侧**展开、**高度撑满视口**，宽度固定 340px。

- **修掉毛玻璃板的边缘伪影**（用户反馈「颜色和背景不一样、边界是清晰的，看起来像
  这儿接了一块，毛玻璃基座的角也是」）。根因：`backdrop-filter` 的模糊核会越过元素
  边界去采样 —— 板边缘那一圈（宽度约等于模糊半径 30px）采到的是**板外**的背景，
  颜色与板内对不上，分界还是硬切的；圆角处同理。
  修法：模糊连同底色一起搬到一块**向外扩 40px** 的伪元素上，再由板的
  `overflow: hidden` 把它裁回原形状，那圈脏采样正好落在被裁掉的部分里。
  板自身不再带 `backdrop-filter`，另加 `position: relative` + `isolation: isolate`，
  保证伪元素定位正确、也不会钻到页面背景之后。
  副作用是好的：玻璃板不再创建「会让 `position: fixed` 改参考系」的祖先，
  弹窗定位又多了一层保险（`append-to-body` 之外）。

- **应用名统一成 `Quill`**：侧边栏品牌区、浏览器标签页、命令行启动提示三处原先只有图标
  旁边那个是小写，三处看着像三样东西。

- **播种扩展到 JSON 条目**：`bootstrap` 原先只管 `prompt/` 与 `skills/` 两个目录，现在还
  负责把出厂的模式与各组补进数据文件。两条路都是**只补不覆盖**：同 id 已经存在就一个字节
  都不动 —— 用户改过的描述、甚至改过的内置提示词正文，都是他的东西。

### 修复

- **e2e 页面冒烟里 `/search` 的断言还停在旧地址**：联网搜索挪进偏好设置（见上文）之后，
  顶栏标题统一由 `SettingsLayout` 给成「偏好设置」，冒烟用例仍按旧页面断言「联网搜索」，
  于是永远失败。改为冒烟 `/settings/search`（按现行设计断言标题），另加一条用例钉住
  旧地址 `/search` 的跳转 —— 书签和历史里的链接不能悄悄断掉。

### 内部

- **路由层更新资源时会从已有条目带过来出厂标记。** 请求体里没有这个字段，直接构造对象会
  把它重置成 `False` —— 那等于「改一次描述就把删除保护拆了」，而且全程没有任何提示。
  模式的 `PUT`、工具组与技能组的 `PUT`、以及提示词的 `save` 都补了这一步。
- `tests/test_defaults.py`：13 条用例覆盖播种内容、幂等、不覆盖用户改动、修回标记、四类
  资源的删除保护，以及上面那个「编辑时丢标记」的坑。

## [0.1.11] - 2026-09-22

### 新增

- **执行权限可以在界面上改了**（顶栏右侧、使用说明旁边那个锁）：沙箱档位三选一 +
  网络开关，**改完下一条命令就生效**、不必重启。

  为什么值得开这个口子：档位天然是按任务变的（读代码 / 改代码 / 装依赖要的边界各不
  相同）。此前它只认 `.env` 的 `SANDBOX_MODE`，改一次就得重启进程 —— 用户的理性选择
  就是干脆一关了之，那才是最坏的结果。

  它**不放**输入框上方那一排：那排是「这一轮」的选择，而沙箱是整台机器的边界，
  放一起会让人以为它只对当前对话生效。弹窗里因此专门写明了「沙箱管**够不够得着**、
  审批管**要不要问**」——这是最容易混的一对概念。

- 弹窗里直说这台机器有没有可用后端。没有的话，选「不隔离」以外的档位会让命令全部
  被拒绝执行，得在用户保存**之前**就讲明白；顶栏入口本身也会变色提示。

- **三档权限按钮一眼可辨**：它原来是个 text 按钮（只有一行字、没有框），摆在顶栏上
  读起来就是一段说明文字 —— 用户不会想到它可点。改成带边框的按钮形态，并按档位换
  文字色：不隔离橙、工作区可写蓝、只读绿（`--sandbox-*`，深浅两套主题各一份）。

  用三种颜色而不是统一灰字，是因为这正是「边界此刻有多松」的读数 —— 要点开弹窗才
  知道，它就失去意义了。不套现成的语义色（success / warning）：三档之间不是
  「好 / 坏 / 危险」的关系，硬套会让「只读」看起来像出了错。

### 变更

- **沙箱档位与网络开关现在只是「默认值」**：`Settings.sandbox_mode` /
  `sandbox_network` 降为回落值，实际生效值由 `sandbox.effective_mode()` 决定
  （界面偏好优先）。`.env` 里已经配过的用户不受影响 —— 界面没设过时仍然听 `.env` 的。
- **「使用说明」移到侧边栏品牌区**（LOGO 与标题右侧）。它讲的是「这个应用怎么用」，
  属于常驻信息；顶栏那侧留给和当前任务真正相关的操作。
- **思考开关移到用量仪表左边**，和上下文 / 缓存读数合成右侧一组（`.meter-group`，
  `margin-left: auto` 写在**组**上）。两者都是「这一轮的运行参数」，摆一起抬头就能
  同时看到 —— 写在「思考」自己身上只会把它单独推到最右，和仪表之间被附件那一排隔开。
- **缓存命中率改成常驻**：没有读数时显示 `—`，而不是整块隐藏 —— 它在功能区最左边，
  藏起来会让整组读数随着「有没有会话、这一轮跑没跑过」横跳。
- 沙箱模块和 `Settings` 的注释里「默认值是 off」的说法，是上一版改成按平台给之后的
  遗留，一并更正。
- 没有可用后端时的引导文案补上了界面上的做法（原来只提 `.env`）。

### 内部

- `tests/conftest.py`：新增全局夹具，把沙箱读的偏好指向临时路径。它现在也会读偏好，
  而那是**开发机上的真实状态** —— 不隔离的话，本机在界面上动过档位之后，会执行命令的
  那批用例就会跟着变行为，而且挂的地方看起来和被测代码毫无关系。
- `server/main.py` 里把沙箱路由按别名导入：它和 `quill_agent.sandbox` 撞名，直接并进
  同一行会盖掉后者，而唯一的用处是启动自检 —— 也就是说只有真启动一次才会炸。

## [0.1.10] - 2026-09-21

### 新增

- **macOS 沙箱后端（Seatbelt）**：`SANDBOX_MODE` 现在在 macOS 上也能真正生效了 ——
  之前配了沙箱的 macOS 机器只会落到「没有可用后端 → 拒绝执行」，开发和测试当场不可用。

  实现走系统自带的 `sandbox-exec`，profile 用 Scheme 语法拼（见 `_seatbelt_profile`）。
  和 bubblewrap 一样是「先从 `(deny default)` 起步，再逐条开口子」；有一条规则语义要记住：
  **后写的覆盖先写的** —— 所以「读盘放开、再把 `~/.ssh` 关掉」必须按这个顺序写。

  `sandbox-exec` 被 Apple 长期标为 deprecated，profile 认的语法也随系统版本变过，
  所以加了探针（`_seatbelt_probe`）：**在系统里躺着不等于能用**。

### 变更

- **沙箱默认档位改为按平台给**：Linux / macOS 默认 `workspace-write`，Windows 默认 `off`。

  README 里原本的计划是「等补上 macOS 后端就把默认值翻成 workspace-write」。macOS 后端
  补上了，但**不能全局翻** —— 默认值对**所有人**生效，而「配了沙箱却没有后端」在本项目里
  是**拒绝执行**（不静默裸跑）。全局翻的话，Windows 用户会**每条命令都被拒**，而他们
  根本没配过沙箱，只会觉得程序坏了。

  ⚠️ **这是行为变更，会影响已有用户**：如果之前在 Linux / macOS 上跑、且没显式设过
  `SANDBOX_MODE`，升级后执行类命令会落进沙箱（工作目录可写、默认断网）。启动自检会报出
  当前档位和用的是哪个后端；想关掉就设 `SANDBOX_MODE=off`。

- **README 补上 Windows 用户走 WSL2 的三道坎**（内核版本、发行版的 userns 限制、
  `bwrap` 与代码位置）—— 那正是 `_bwrap_probe` 会报的错，文档里先讲一步，
  用户看到报错时就知道往哪走。同时说清了「原生 Windows 沙箱为什么不好做」
  （`AppContainer` 的能力模型是全有或全无、`Windows Sandbox` 太重、完整性级别粒度太粗；
  真要做得上「受限 Token + 合成 SID」的组合拳）。

- **启动自检会报出用的是哪个后端**（`…后端 Seatbelt`）。两个后端的隔离语义有几处
  不一样，出问题时第一件事就是确认跑的是哪一个。

- **两个平台上有意的语义差异，都写进了注释和 README**：bubblewrap 能给沙箱一个干净空的
  `/tmp`，而 Seatbelt 只能「允许 / 拒绝某个路径」—— 所以 macOS 那边是**放行**
  `$TMPDIR` 和 `/tmp`，不是隔离它们。不这么做的话 python 会直接报「没有可用的临时目录」，
  构建和跑测试全线失败。临时文件不是秘密，而少了它沙箱根本没法用。

  同理，`/dev/null` 这类设备写入要单独开口子（`2>/dev/null` 太常见），
  `mach-lookup` 必须放行（macOS 的进程间服务，不给的话大半个系统调用起不来）。

- **测试的「真实隔离」层不再只认 bubblewrap**：三个用例改用「本机有可用后端」这个条件，
  所以 macOS 上也会真跑一遍（写工作目录成功、写外面失败、网络不通）。
  另有两处跟着改：探针 fixture 现在**两个后端一起 mock**（只 mock 一个的话，在 macOS 上
  会落到 Seatbelt 分支，而那条路真能用 —— 用例就验不到「没后端时拒绝」了）；
  「写外面」的目标从 `tmp_path.parent` 换成 `/` 下的路径（前者住在 `$TMPDIR` 里，
  而那正是被放行的地方）。

## [0.1.9] - 2026-09-21

### 新增

- **任务页显示「跑了多久 / 跑到第几圈」**（阶段提示后面那行小字，形如 `3:07 · 第 9/30 轮`）。

  起因是一次实测：一个 PPT 任务跑了 12 分钟，界面上能看到的只有「正在思考…」这一句 ——
  跑了多少、还剩多少余地、要不要按停止，全靠猜。现在用时每秒走字，轮次由新增的
  `Round` 事件播报（每轮请求前一次；`index > total` 表示预算用尽后的收尾轮，界面显示
  「收尾中」）。

  轮次刻意由**我们自己数**，不依赖服务端：`usage` 得等对接方返回用量，而有些服务不给；
  轮次是我们数的，一定有。所以「进度可见」这件事只能靠它。

- **等用户回答时给出醒目标记**：那行阶段文案会高亮，浏览器标签页标题变成
  「⚠ 等待你的确认」。

  同一次实测的第二课：`spawn_agent` 的确认卡片静静躺在浮层里，人（包括我自己）盯着上面
  那行「正在思考…」看了十分钟都没注意到它 —— 而模型一直在等，最后等到 `DEFAULT_TIMEOUT`
  被判成「拒绝」。改标题是为了**切到别的标签时**也能看见。

## [0.1.8] - 2026-09-21

### 修复

- **到工具轮次上限时，模型会把调用写进正文**：预算用尽后最后一次请求不再带
  `tools` 字段，但**没有任何消息告诉过模型这件事** —— 它的历史里全是 `tool_calls`，
  system 提示词里也还列着工具，于是它继续按调用格式输出；这一轮没有通道，
  那段调用就漏进了 `content`。

  实测（2026-09-21 的 PPT 任务）：`deepseek-flash` 前 10 轮 18 个工具调用
  **全部规范**，恰恰是收尾那一轮退化的。所以这不是「模型不行」——用同一份代码的
  其它模型一样会漏。现在收尾轮会补一句说明（`FINAL_ROUND_INSTRUCTION`），
  明确告知工具已撤走、请基于已有信息作答。

- **这个场景的提示语在甩锅给模型**：原来不管哪种收尾失败都说「模型把工具调用
  写成了普通文本，可以换一个函数调用更稳定的模型」。可收尾轮漏调用的根子在
  「没告诉模型工具没了」，换了模型照样漏。现在这种情况报的是轮次上限。

- **子代理跑得久了会被前端当成死连接掐掉**：子代理的动静是按**动作**播的（调了哪个
  工具），两个动作之间可能隔着很久 —— 模型在长思考、在长篇输出、工具在跑。那段时间
  前端一个字节都收不到，而它有一条 360 秒的静默保护（`web/src/api/chat.ts` 的
  `IDLE_TIMEOUT_MS`），会把「一直没数据」当成死连接，掐掉整条流。

  实测（2026-09-21）：一次 `spawn_agent` 跑了整 360 秒，期间一个事件都没往外播，
  这一轮白跑 —— **子代理干得越认真，越容易被自己人误杀**。那道保护的用意没错
  （防「对端睡了」），错的是这里给它的信息太少。

  现在子代理做两件事：① 播 `ToolStart`（执行**前**）而不是 `ToolStep`（执行**后**）——
  该被看见的是工具**跑着**的那段时间，播「跑完了」对「是不是卡住了」没有帮助；
  ② 长时间只有正文 / 思维链时，每 30 秒补一句「子代理正在工作…」
  （`_HEARTBEAT_SECONDS`）。

- **事件流静默时会被客户端当成死连接掐掉**（上一条的根本解）：`queue.get()` 没事件时
  会一直挂着 —— 那是长连接的常态，可对客户端来说「一直没有数据」和「连接死了」是分不清
  的。前端为此设了静默保护，撞上就 `reader.cancel()`，服务端这一轮跟着被取消。

  实测发现上一条并不够：子代理那次静默发生在**等模型响应的阶段**，此时生成器一个
  `yield` 都没有，子代理层的心跳代码根本没机会执行。

  所以 SSE 层也加了心跳：静默超过 60 秒（`SSE_HEARTBEAT_SECONDS`）补一行注释
  （`: keep-alive`）。协议规定以 `:` 开头的行客户端应当忽略 —— 前端确实忽略了
  （`parseBlock` 认不出 `event:` / `data:` 就跳过），但那一行字节照样算「收到了数据」。

  两条是互补的：前一条是「让该播的动静都播出去」，这一条是「兜住任何没播到的静默」——
  长思考、慢工具、子代理在跑，都属于后者。

### 变更

- **`MAX_ITERATIONS` 从 10 提到 30，并从「主闸门」降为「兜底」**。

  次数管不住成本 —— 两次请求的开销能差两个数量级（读一个 3 行文件 vs 装一个包），
  按次数卡住的是正常的长任务，真跑飞了反而未必拦得住。该当主闸门的是 token 预算，
  定位与 Claude Code 的 `maxTurns` / `maxBudgetUsd` 对齐。

  10 这个数字是被 plan 流程逼出来的，对「探路型」任务太紧：光摸清项目结构、
  读文档、确认工具链就能吃掉十轮，正事还没开始 —— 那个 PPT 任务就是这样，
  十轮全花在探路上，PPT 一行没写就被迫收尾。

- **工具轮次上限现在可配**：偏好设置 → 对话 →「轮 / 单轮对话」，留空用默认值（30）。
  任务的正常轮次差得很远，硬定一个数只能两头不讨好。

### 新增

- **接近预算时提醒模型收尾**（`BUDGET_WARN_RATIO` / `BUDGET_WARNING`）：token 用到
  上限的**八成**就往上下文里补一条提示，让模型把剩下的活收拢进预算之内。这是
  Claude Code 里 `TOKEN_BUDGET` 的做法。

  硬停很亏：那一刻工具刚跑完、结果也回来了，**却没有下一次请求去消化它**。
  提前提醒，模型能自己收口。提醒只发一次 —— 反复发既占上下文，也只会让模型
  一直念叨收尾。

## [0.1.7] - 2026-09-21

### 新增

- **任务页显示缓存命中率**（上下文仪表盘**左边**的 `xx% 缓存`）。它读的是
  `cached_tokens / context_tokens` —— 分子分母来自**同一次请求**，与旁边的上下文读数
  同一个口径，两个数放在一起才说得通。还没有可用读数时**整块不显示**，而不是填一个
  0%（「不知道」和「真没命中」是两回事）。

  后端从用量里抠这个数时两种字段名都认：OpenAI / Ollama 的
  `prompt_tokens_details.cached_tokens`，以及 DeepSeek 的 `prompt_cache_hit_tokens`。
  实跑验证：`{"context_tokens": 80, "cached_tokens": 3}` → 界面显示 `4% 缓存`。

## [0.1.6] - 2026-09-21

### 新增

- **任务页可以关掉模型的思考**（模型选择旁边的「思考」开关）。小模型常常一思考就把输出
  预算花光、正文一个字都给不出来 —— 实测 `qwen3.5-9b-rp` 回答「1+1等于几？」时，开着思考
  是 **180 个 reasoning 事件对 1 个 text 事件**，关掉之后是 **0 对 78**。

  关闭时带 `reasoning_effort="none"`（OpenAI 标准字段，Ollama 兼容层实测有效；
  `chat_template_kwargs.enable_thinking` 实测**无效**）。它是**全局偏好**
  （`preferences.json` 的 `thinking` 键，默认开），和模型选择同一个口径 ——
  「要不要思考」是跟着模型走的，不是某一个任务的属性。

  不认识这个字段的服务会直接 400，所以发请求是**三级降级**：
  `用量统计 + 关思考` → `只关思考` → `什么都不带`。关不掉思考只是小事，
  让整轮对话失败才是大事。

## [0.1.5] - 2026-09-21

### 新增

- **CI（GitHub Actions，`.github/workflows/ci.yml`）**：push / PR 时自动跑后端
  `ruff` + `pytest` 与前端 `npm ci` + `typecheck` + `build` + `e2e`。此前这些只在手动
  `make test` 时才跑，回归全靠人记得跑一遍。
- **前端 e2e 扩到「逐页冒烟」**（`web/e2e/pages.spec.ts`）：11 个页面每个都验「能打开、
  标题正确、控制台不报错」—— 等价于把「手工点一遍所有页面」自动化，专盯前后端契约漂移。
  `mock-api.ts` 顺带补齐了各页面用到的端点（注意同一个 `/api/models` 被两个页面用两种
  口径读，`options` 与 `configs` 都要给）。

### 修复

- **README 的工具表漏了 `recall_history`**（表头写 26 个，实际 27 个）。功能本身没缺，
  3.4 节有它的详细说明，只是表格没列 —— 这类「文档数字悄悄过期」以后靠 CI 之外的人眼
  盯着，补上了就好。

## [0.1.4] - 2026-09-21

### 新增

- **前端 e2e 冒烟测试**（Playwright，`web/e2e/`）。覆盖任务页最容易出错、也最难手工
  穷尽的几处状态流转：发消息后的流式正文与工具步骤渲染、运行中输入框禁用与「停止这一轮」、
  请求失败后必须解锁输入框（不能卡在 busy）。
  - 所有 `/api/*` 都在测试里拦掉（`e2e/mock-api.ts`），**不需要后端、也不需要模型** ——
    后端已有单测覆盖，这里要的是「界面 + SSE 解析 + 渲染」这条链路的回归保护。
  - 用系统已装的 Chrome（`channel: 'chrome'`），省掉 `playwright install` 的下载。
  - `make e2e` / `npm run e2e` 运行。CI 上没有系统 Chrome 时改用 `chromium` 并先装一次。

## [0.1.3] - 2026-09-21

### 修复

- **「工具调用漏进正文」的识别补上了它实际出现的两种格式**。原先只认三种带标签的调用块
  （DeepSeek DSML、`</invoke>`、`<function_calls>`），实测发现真实泄漏的格式是另外两种，
  一个都没覆盖：
  - Qwen 系标准的 `<tool_call>{…}</tool_call>`；
  - **裸 JSON**（`[{"name": …, "arguments": …}]`）—— `qwen3-4b-function-calling-pro` 在
    真实工具组下稳定吐这个形状，没有任何标签，前缀匹配认不出来。

  裸 JSON 改用形状启发式（`_looks_like_tool_call`），且**仅当工具菜单非空、这一轮
  `tool_calls` 为空**时才算；刻意保守（要求以 `[` / `{` 开头且同时出现 `name` 与
  `arguments`/`parameters`），避免把正常的 JSON 回答当成泄漏。
  不认的话，用户只看到一串 JSON、工具一个都没跑，也不知道发生了什么。

## [0.1.2] - 2026-09-21

### 修复

- **流连接僵死会让界面永久卡住**（对端休眠 / 网络中断时最容易遇到）。三处配合才修掉：
  ① `reader.read()` 在没数据时挂着是长连接的**常态**（等模型吐第一个字、等工具跑完），但连接
  僵死时它同样挂着 —— TCP 半开连接不会立刻报错。现在读取带**静默超时**
  （`IDLE_TIMEOUT_MS`，360 秒，照后端工具执行上限 300 秒留的余量），一段时间没有任何数据就
  报错收尾；
  ② `finally` 里的 `reader.cancel()` 被 `await` 了 —— 僵死连接上它可能**永远不 resolve**，
  把整个收尾（连同超时抛出的那个异常）一起拖住。这正是「超时也救不回来、只能刷新」的最后
  一道锁，改成不 await（尽力而为，失败也随流对象被 GC 回收）；
  ③ 界面状态按「这个会话现在还有没有 run」重算，不再无条件解锁（见下条）。
- **同一会话并发运行时状态会错乱**：`runs`（会话 → 运行）里同会话只能存一个 run，同会话再发一条
  会把前一条的记录**覆盖**掉，而先结束的那一轮 `finally` 又会把后来者的记录删掉 —— 两轮互相踩。
  现在 `sendMessage` 开头直接拦住同会话并发（拦在消息上屏**之前**），`finally` 只清理自己那一份，
  并按「这个会话现在还有没有 run」重算界面状态。
  真人点不出这个并发（输入框在 busy 时禁用），这道守卫拦的是绕过界面直接调用那条路。

## [0.1.1] - 2026-09-21

### 修复

- **删除提示词未级联，会在提示词组里留下删不掉的悬空引用**：引用按 id 存，只删正文的话，
  组里那条 id 在界面上连名字都显示不出来（`refsOf` 查不到就不渲染），保存时又被原样写回 ——
  用户没有任何手段处理它。现在删除会级联到所有引用它的组（`PromptGroupStore.drop_prompt`），
  并把动过的组名回传（`used_by`）、在成功提示里点名。更早删掉的那批悬空引用由启动自检清理
  （`_prune_dangling_prompt_refs` → `PromptGroupStore.prune_missing`），幂等、无变化不写盘。

### 新增

- **工具执行前播报 `tool_start` 事件**：`ToolStep` 是执行**完之后**才产的，而中间那几秒到
  几十秒（联网搜索、扫大目录、跑命令）事件流是静默的。现在 agent 在执行前先播一条
  （只带工具名、不落盘），界面据此显示「正在执行 xxx…」。
- **任务页显示这一轮跑到哪一步**（`RunPhase`，纯前端事件驱动状态机）：正在连接 / 等待模型
  响应 / 正在思考 / 正在生成 / 正在执行工具 / 等待你的回答 —— 回答的是「是不是卡了」。
- **侧边栏标记正在跑的会话**（转圈图标，`session.runningIds`）：运行跟着会话走，切走之后
  也要能看出那个任务还在不在跑。

## [0.1.0] - 2026-09-21

首个可用版本。

### 架构

- **业务层与界面完全分离**：`src/quill_agent/` 是纯 Python，不 import 任何界面框架
  （HTTP、前端、终端 UI），可被 Vue 前端、CLI、脚本、测试任意复用。
- **分层单向**：`web/ ──HTTP/SSE──> server/ ──> src/quill_agent/`。
- **数据全在本地、全是明文**：会话 JSONL、配置 JSON，可读可改可备份。

### 能力

- **Agent 循环**：流式输出、工具调用、思维链、并行工具结果回填、轮次预算与强制收尾、
  运行中取消、执行前确认、模型主动提问、计划审批、子代理。
- **五类可配资源**：提示词、工具、技能、记忆、模式 —— 都是数据，改完即生效，
  不需要动代码。
- **上下文三层管理**：
  - 按 token 预算截断历史（依据模型窗口，没配窗口时回退条数上限）
  - 早期对话压成滚动摘要（原文一句不删，失败自动降级）
  - `recall_history` 工具按关键词检索全部历史原文
- **单轮开销上限**：在「偏好设置」里设 token 阈值，超过即停。
- **联网搜索**：可插拔后端（Tavily / 博查），「搜」与「读」拆成两个工具。
- **多协议**：OpenAI Chat Completions、Anthropic Messages、Ollama（本地，走兼容层）。
- **安全边界**：工作目录 PathGuard、bubblewrap 沙箱（Linux）、危险命令硬拒绝、
  需要确认的工具按组配置。
- **健壮性**：跨进程文件锁 + 原子写；单条坏记录不影响其余数据；坏行跳过。

### 界面

- Vue 3 + Vite + TypeScript 前端，与后端通过 REST + SSE 通信。
- 页面：任务、模式、提示词、API 设置、联网搜索、工具、技能、记忆、归档、用量、偏好设置。
- 任务页：上下文用量仪表盘、流式跟随、工具步骤与思考过程折叠、
  **浮层**（子代理看板 / 待确认卡片 / 可折叠任务清单，滚动时不丢失）。
- 会话导出：Markdown（给人读）或 JSON（给程序用）。
- 单进程运行：后端托管前端产物，`make api` 一条命令即可用。

### 本次收尾的修补

- 搜索类工具曾绕过 `PathGuard`（工作目录里的软链指向外面时可被读出）。
- `run_command` 曾全量缓冲输出，一条 `yes` 就能把内存顶上去（改为边读边丢）。
- 存储层曾因单条坏记录导致整页打不开（改为逐条校验、坏的那条跳过）。
- 快速切换会话时，先发后到的响应会覆盖当前会话。
- 切换会话曾中断正在跑的那一轮（改为「运行跟着会话走」）。
- `edit_file` 缺文件大小上限；`read_skill` 缺名称校验。
- 本轮工具结果未按上限截断（读一个大文件会当场把上下文撑大几倍）。
- 服务端异常原文曾回显到浏览器（改为写日志 + 一句通用提示）。
- 模型给出的嵌套量词正则可能引发灾难性回溯（现在直接拒绝）。
- Windows 上命令超时会抛 `AttributeError`（`os.killpg` 不存在），改用 `taskkill /T`。
- 数据文件写入无锁、非原子（先截断再写），并发时会丢更新。

### 移除

- Streamlit 旧界面（`app/`）及其可选依赖组 —— Vue 前端已完全取代它，
  连带去掉 pandas / pyarrow / protobuf 等几百 MB 传递依赖。
